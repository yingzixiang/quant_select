# 三引擎收敛设计方案（quant_select）

> 面向 zale 的决策文档：是否/如何把中线、短线、日内三套引擎收敛到 1–2 套。
> 生成时间：2026-09-08　｜　配套架构文档：`ARCHITECTURE.md`

## 1. 现状与问题

三套引擎并存、各自独立（独立 CLI / 配置 / 输出 / 回测），仅共享缓存层：

| 引擎 | 周期 | 决策核心 | 数据要求 | 实盘路径 |
|------|------|----------|----------|----------|
| 中线月度 `core/` | 月度调仓 | 21 项宏观择时 + 12 微观因子 | 日线 + 财报 + 宏观 | 有（低频、易人工执行） |
| 短线 `short_term/` | T+1→T+3 | 47 因子 + XGBoost + 五级硬约束 | 日线 + 资金流 + 指数 | 有（broker 层已就绪） |
| 日内 `intraday/` | 分钟级底仓回转 | 十一层规则引擎 | **分钟线 + 盘口 + 板块分钟** | **缺**（无分钟实盘通道） |

**维护痛点**：
- 三套各写各的数据获取、因子、回测引擎，重复度高（都拉 A 股日线/列表/指数）。
- 回测口径不一：`core/trader.py` 等权无成本，`short_term` 有手续费/滑点，`intraday` 另有独立引擎。
- 日内引擎数据要求最苛刻：akshare 分钟数据能力弱（`DATA_DEGRADATION` 里五档盘口/逐笔/B-S 均标记不可用），且它依赖「底仓」——底仓来源正是中线/短线，逻辑上不独立。

## 2. 三套引擎的真实关系

不是互相替代的横向并列，而是**纵向互补**：

```
中线月度  ── 定资产配置与底仓（股债比例、风格、稳健底仓）
    │
短线 T+1~3 ── 在底仓之上做择时增强（高频换手、博超额收益）
    │
日内 T+0  ── 对已有底仓做日内增厚（不新增隔夜仓，纯回转）
```

- 砍掉中线 → 失去资产配置层，全裸择时，回撤无锚。
- 砍掉短线 → 失去超额收益主要来源（最活跃、信号最密、最贴近「选股」主线）。
- 砍掉日内 → 失去的只是「底仓增厚」，但它的**数据与实盘前提当前都不成立**。

## 3. 收敛目标与原则

- **目标**：把维护面从 3 套降到 1–2 套，且保留策略结构的纵向完整性。
- **原则**：
  1. 复用优先于删减——先抽共性底座，再决定砍哪套；
  2. 实盘可行性优先——没有实盘通道的引擎不投入维护；
  3. 分阶段、可回退——每阶段跑通回测基准再进下一步，git 保留历史随时回滚。

## 4. 候选方案对比

| 方案 | 做法 | 优点 | 缺点 | 适用 |
|------|------|------|------|------|
| **A. 砍到 1 套（只留短线）** | 删除中线/日内，中线降级为短线的一个「环境过滤」模块 | 维护面最小 | 失去资产配置层，回撤无锚；宏观择时能力浪费 | 只想专注短线高频、资金小 |
| **B. 砍到 2 套（中线+短线，砍日内）** | 冻结/归档 `intraday/`，保留中线做底仓 + 短线做择时 | 纵向结构完整、实盘路径都通、维护面-33% | 失去日内增厚（但该能力当前本就不成立） | **推荐**（见下） |
| **C. 架构收敛、策略全保留** | 抽共享 framework，三套变三个插件 | 策略价值最大、长期扩展最好 | 迁移成本最高，日内仍是负债 | 团队化、有分钟数据源后 |

## 5. 推荐方案：B + 同步抽共享底座

**主推「方案 B」**：先砍掉 `intraday/`（归档，不物理删除，git 保留），保留「中线月度 + 短线」两套；同时按下面目标架构抽出共享底座，让两套真正复用，而不是继续各写各的。

理由（一条条对齐 zale 的实盘诉求）：
1. 日内引擎当前「无分钟实盘通道 + akshare 分钟数据不可用」，留着是纯维护负债，不产生收益。
2. 中线负责底仓/仓位（低频、易执行、有港股覆盖），短线负责择时（已有 broker 下单层），两者都已有清晰实盘路径。
3. 两套周期互补（月度 + 日），砍掉任一套都会损失策略结构的完整性；砍日内不损失当前可兑现的能力。

## 6. 目标架构（收敛后）

```
quant_select/
├── base/                     # 共享底座（新增，抽取三套共性）
│   ├── data/                 #   统一数据获取：日线/资金流/指数，多源回退（东财→腾讯→新浪）
│   ├── cache.py              #   缓存（现有 core/data_cache.py 上移）
│   ├── factors/              #   因子库：短线 Alpha 为日频主力，中线基本面为月度
│   ├── backtest.py           #   统一回测引擎：信号→成交→绩效（统一成本/滑点口径）
│   ├── risk.py               #   统一风控（前置过滤 + 后置校验）
│   └── broker/               #   下单层（现有 broker/ 上移，已含抽象+模拟盘）
├── strategies/
│   ├── swing/                #   短线 T+1~3（现 short_term 改造）
│   │   ├── strategy.py       #     实现 Strategy 接口
│   │   ├── factors.py        #     Alpha158 因子
│   │   ├── filters.py        #     五级硬约束
│   │   └── model.py          #     XGBoost 训练/预测
│   └── monthly/              #   中线月度（现 core 改造）
│       ├── strategy.py       #     实现 Strategy 接口
│       ├── macro_timing.py   #     宏观择时
│       └── micro_factors.py  #     12 因子
├── config/                   # 保留，按策略拆子配置
├── main.py / cli.py          # 统一 CLI：python -m strategies.swing --mode select ...
└── archive/                  # intraday/ 归档（git 保留历史，不参与维护）
```

**统一 `Strategy` 接口**（两套策略都实现）：
```python
class Strategy:
    def generate_signals(self, date) -> list[Signal]   # 选股/择时信号
    def build_portfolio(self, signals) -> Portfolio     # 组合/仓位
    def risk_check(self, portfolio) -> RiskResult       # 风控
```
回测引擎、broker 下单、盯盘都只依赖这个接口，**新增第 N 套策略的成本降为「只写策略本身」**。

## 7. 分阶段迁移路径（可回退）

| 阶段 | 内容 | 验收 | 风险 |
|------|------|------|------|
| P0 | 冻结 `intraday/`：移入 `archive/`，README 标注归档，不物理删除 | 现有中线/短线不受影响，git 历史完整 | 极低 |
| P1 | 抽数据层 `base/data/`：统一三套的 fetch（日线多源回退） | 短线 `--mode select` 输出与迁移前一致 | 低 |
| P2 | 抽统一回测引擎 `base/backtest.py`：统一成本/滑点口径 | 短线回测指标与迁移前一致 | 中（口径对齐需核对） |
| P3 | 策略插件化：swing/monthly 实现 Strategy 接口，接入统一 CLI | 两套各模式跑通 | 中 |
| P4 | broker 打通实盘：选股→自动下单→止损止盈盯盘 | 模拟盘全链路 + 实盘渠道接入 | 中（依赖渠道决策） |

## 8. 成本 / 收益 / 风险

- **收益**：维护面 -33%（3→2）；新策略接入成本大降；回测/风控/下单口径统一，不再三套各自漂移；为「全自动实盘」铺平底座。
- **成本**：P0~P3 约需一轮专注重构，核心风险在 P2 回测口径对齐（要拿迁移前指标做基准回归）。
- **风险控制**：每阶段独立提交、git 可回滚；`archive/` 保留日内代码，未来若接入分钟数据源（如券商 L2/分钟接口）可低成本复活。

## 9. 需要 zale 决策的点

1. **是否采纳方案 B（砍日内、留中线+短线）**？还是你倾向只留短线（方案 A）？
2. **资金量级与目标**：决定是否需要「中线做底仓」这一层——若资金小、只做短线高频，方案 A 更轻。
3. **日内是否未来会做**：若确定要做分钟级，需同时确认分钟数据源/实盘通道（当前没有），否则 P0 归档后不复活。
4. **重构节奏**：P0~P3 我按阶段推进并每阶段回帖验收，还是你先看这份方案拍板后再动工？
